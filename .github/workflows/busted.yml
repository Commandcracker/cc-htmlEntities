name: Busted

on:
    workflow_dispatch:
    pull_request:
    push:
        paths: ["**.lua"]

jobs:
    busted:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout 🛎️
              uses: actions/checkout@v3

            - name: Run Busted ✅
              uses: lunarmodules/busted@v2
